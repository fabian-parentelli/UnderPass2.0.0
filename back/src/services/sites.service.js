// import { ticketRepository } from "../repositories/index.repositories.js";
import { SitesNotFound } from '../utils/custom-exceptions.utils.js';

const newSite = async (imagesUrl, site) => {
    console.log(imagesUrl);
    console.log(site);
};

export { newSite };