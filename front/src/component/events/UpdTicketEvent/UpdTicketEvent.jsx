import './updTicketEvent.scss';

const UpdTicketEvent = ({ event, closedInfo, setEvents, events }) => {

    

    return (
        <div className='updTicketEvent'>
            UpdTicketEvent
        </div>
    );
};

export default UpdTicketEvent;