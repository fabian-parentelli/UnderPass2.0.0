import './confirmEvent.scss';

const ConfirmEvent = ({ values, setValues, setLoading, setProgres }) => {

    return (
        <div className='confirmEvent'>
            ConfirmEvent
        </div>
    );
};

export default ConfirmEvent;